# Core
anthropic>=0.40.0       # Claude API for classification
mcp>=1.27.0             # MCP SDK for Heptabase connection (>=1.27.0 for OAuth fix)

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Dev
ruff>=0.8.0             # Linting + formatting
